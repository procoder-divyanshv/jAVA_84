public class MyPrintMain {
    public static void main(String[] args) {
        MyPrint obj= new MyPrint();
        obj.print();
        obj.show();
    }
}

//java do not support the multiple class extends in the defination because if the classes to be
//extended and it conataines same fuction in it then java complier can confuse which function to call so that is why java compiler do not support this type of implementation.

// class can extends other class
//class can implements the interface
// one interface can extends other interface

//one single class can impliments multiple interfaces
//one single interface can extends multiple interfaces


//Encapsulation: It is defined as the wrapping up data in a single unit
